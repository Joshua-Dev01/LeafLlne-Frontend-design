// must export array

import { Settings } from "../settings";

const SettingsRoutes = [
 
   
      {
        index: true,
        element: <Settings />,
      },
      {
        path: "projects",
        // element: <Projects />,
      },
  
];

export default SettingsRoutes;
